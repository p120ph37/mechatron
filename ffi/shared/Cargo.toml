[package]
name = "mechatron-ffi-shared"
version = "0.0.0"
edition = "2021"

[lib]
name = "mechatron_ffi_shared"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"

[target.'cfg(target_os = "macos")'.dependencies]
libc = "0.2"
