/**
 * ffi mouse backend — portal/libei variant (stub).
 *
 * Will use libei via dlopen for synthetic pointer events on Wayland
 * compositors.  Falls back cleanly to the next backend entry.
 */

throw new Error("ffi/mouse-portal: libei backend not yet implemented");
