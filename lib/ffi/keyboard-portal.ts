/**
 * ffi keyboard backend — portal/libei variant (stub).
 *
 * Will use libei via dlopen for low-latency synthetic key events on
 * Wayland compositors.  Falls back cleanly to the next backend entry.
 */

throw new Error("ffi/keyboard-portal: libei backend not yet implemented");
